# With a physical Android device over USB, run: adb reverse tcp:4319 tcp:4319
EXPO_PUBLIC_MOBILE_API_URL=http://127.0.0.1:4319
# Required only when the backend is configured with MOBILE_API_TOKEN.
# EXPO_PUBLIC_MOBILE_API_TOKEN=use-the-same-mobile-api-token
